using UnityEngine;
using System.Collections;

public class StayStraight : MonoBehaviour {

	
	// Update is called once per frame
	void Update ()
    {
        transform.rotation = Quaternion.identity;
    }
}
